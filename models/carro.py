import os
import json

class Carro:
    class Carro:
        def __init__(self, marca, modelo, ano, placa, km_atual, user_id, id=None):
            self.id = id
            self.user_id = user_id
            self.marca = marca
            self.modelo = modelo
            self.ano = ano
            self.placa = placa
            self.km_atual = km_atual

